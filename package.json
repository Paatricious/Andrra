{
  "name": "andrra",
  "private": true,
  "description": "Andrra — sleep-screen wallpapers for CrossPoint e-readers (plugin + conversion pipeline)",
  "scripts": {
    "test": "node --test test/*.test.mjs"
  }
}
